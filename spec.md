目的：demo验证mirrod的使用
方法：
设计一个简单的java web app，接收从外部发送的请求，需要使用到mysql作为database。整套服务部署在本地的kind k8s中。
在k8s外的本地服务可以通过mirrod来接入k8s中的数据库并处理特定header的请求。其他请求还是k8s中的服务处理。
结果：
产出自动化的脚本验证这个流程
详细的mirrord在本项目中的使用文档，接入需要哪些东西，怎么做？目的是让开发者可以快速学会连上k8s集群进行local debug；
